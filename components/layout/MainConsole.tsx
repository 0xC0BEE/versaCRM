import React from 'react';

// This component is deprecated. Its functionality has been moved into App.tsx.
// It is now an empty placeholder to resolve any potential lingering import issues in some dev environments.
const MainConsole: React.FC = () => {
    return null;
};

export default MainConsole;