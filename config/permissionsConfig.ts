// This file is intended to hold a centralized configuration or enum for all permissions.
// Although currently unused as permissions are defined directly in the `types.ts` file,
// it is kept for potential future refactoring to centralize permission management.
