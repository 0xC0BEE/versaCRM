/* index.css - Global styles */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  color: rgb(var(--color-light-text));
}

.dark body {
    color: rgb(var(--color-dark-text));
}

/* Ripple effect for buttons */
.ripple {
  position: relative;
  overflow: hidden;
}
.ripple span.ripple-effect {
  position: absolute;
  border-radius: 50%;
  transform: scale(0);
  animation: ripple-animation 400ms linear;
  background-color: rgba(255, 255, 255, 0.4);
}

@keyframes ripple-animation {
  to {
    transform: scale(4);
    opacity: 0;
  }
}

/* Entrance Animations */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.animate-fadeInUp {
  animation: fadeInUp 0.5s var(--transition-soft) forwards;
  animation-fill-mode: backwards; /* Apply 'from' state before animation starts (during delay) */
}

/* Form Validation Animation */
@keyframes shake {
  10%, 90% { transform: translate3d(-1px, 0, 0); }
  20%, 80% { transform: translate3d(2px, 0, 0); }
  30%, 50%, 70% { transform: translate3d(-4px, 0, 0); }
  40%, 60% { transform: translate3d(4px, 0, 0); }
}
.animate-shake {
  animation: shake 0.82s cubic-bezier(.36,.07,.19,.97) both;
}

/* Add Tailwind directives */
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer components {
  .glass-overlay {
    @apply bg-white/30 backdrop-blur-xl border border-white/20;
  }
  .dark .glass-overlay {
    @apply bg-dark-card/60 backdrop-blur-xl border border-dark-border/50;
  }
}
